% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_sample.R
\name{simCytSample}
\alias{simCytSample}
\title{Simulate all cytokine combinations for a set of stimulation conditions for a single biological sample}
\usage{
simCytSample(
  nMarker,
  nCondition,
  nCluster,
  nCellByCondition,
  transformationFunc,
  mixtureType = "gaussianOnly",
  meanExprMat = NA,
  clusterLabelVec = NA,
  probVecUns,
  probExact,
  probResponseVecByStimCondition = NULL,
  conditionPerturbationSd = 0,
  clusterPerturbationSd = 0,
  covEvMin = 1,
  covEvMax = 2,
  meanExprMatReference = NULL,
  stimMeanShift = 0,
  stimSdMultiplier = 1
)
}
\arguments{
\item{nMarker}{Integer. Number of markers/dimensions.}

\item{nCondition}{Integer. Number of stimulation conditions (must be >= 2).}

\item{nCluster}{Integer. Number of clusters (must be a power of 2 between 2 and 1024).}

\item{nCellByCondition}{Integer or numeric vector. Number of cells per condition. If a single
value, it is recycled for all conditions.}

\item{transformationFunc}{Function. Post-simulation transformation to apply
to simulated data (for example identity or a StimGate-style gamma
transformation).}

\item{mixtureType}{Character. Type of mixture distribution: "gaussianOnly", "tOnly", or
"tPlusGauss".}

\item{meanExprMat}{Numeric matrix. Cluster mean vectors (nCluster x nMarker).}

\item{clusterLabelVec}{Character vector. Labels for each cluster (length nCluster).}

\item{probVecUns}{Numeric vector. Probability distribution for unstimulated condition
(length nCluster, sums to 1).}

\item{probExact}{Logical. If TRUE, use exact probabilities for cluster assignment; if FALSE, sample from a multinomial distribution.
Default is \code{FALSE}.}

\item{probResponseVecByStimCondition}{List. Probability response vectors for each stimulated
condition (length nCondition - 1, each of length nCluster).}

\item{conditionPerturbationSd}{Numeric. Standard deviation of condition-level perturbations
to cluster means.}

\item{clusterPerturbationSd}{Numeric. Standard deviation of cluster-level perturbations
within each condition.}

\item{covEvMin}{Numeric. Minimum eigenvalue for cluster covariance matrices. Default is 1.}

\item{covEvMax}{Numeric. Maximum eigenvalue for cluster covariance matrices. Default is 2.}

\item{meanExprMatReference}{Numeric matrix. Reference cluster mean matrix.}

\item{stimMeanShift}{Numeric scalar. Deterministic signed additive mean shift
applied to stimulated conditions post-transformation. Default is 0.}

\item{stimSdMultiplier}{Numeric scalar. Multiplier applied to stimulated
conditions post-transformation standard deviation around component means.
Default is 1.}
}
\value{
A list with \code{flowFrameList} and \code{conditionLabelsList}.
}
\description{
Simulate a set of stimulation conditions (e.g., stimulated and unstimulated) for
a single biological sample, where the first condition is always the unstimulated condition.
}
\keyword{internal}
