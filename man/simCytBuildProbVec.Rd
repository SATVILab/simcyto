% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{simCytBuildProbVec}
\alias{simCytBuildProbVec}
\title{Build a baseline probability vector for a binary marker-combination scenario}
\usage{
simCytBuildProbVec(nMarker, weights = NULL, defaultWeight = 1)
}
\arguments{
\item{nMarker}{Integer scalar. Number of binary markers. Determines the
vector length as \code{2^nMarker}.}

\item{weights}{Optional named numeric vector mapping cluster label strings to
unnormalised weights. Cluster labels use the convention \code{"F1-F2+F3-..."}.
If \code{NULL}, all populations receive \code{defaultWeight}.}

\item{defaultWeight}{Numeric scalar. Weight assigned to populations not
present in \code{weights}. Default is \code{1}.}
}
\value{
A normalised numeric probability vector of length \code{2^nMarker}.
}
\description{
Construct a probability vector of length \code{2^nMarker} from per-population
weights. This is a convenience helper for scenarios with many markers where
manually writing a vector of length \code{2^nMarker} is error-prone.
}
\details{
The function accepts an optional named numeric vector \code{weights} that maps
cluster labels (as returned by \code{\link[=simCytBuildScenario]{simCytBuildScenario()}}) to unnormalised
weights. Any cluster not named in \code{weights} receives weight \code{defaultWeight}.
The resulting vector is normalised to sum to 1.
}
\examples{
# Uniform baseline for 3 markers (8 populations)
p <- simCytBuildProbVec(nMarker = 3)
length(p) == 8
sum(p)

# Put most probability on the all-negative population
p2 <- simCytBuildProbVec(
  nMarker = 3,
  weights = c("F1-F2-F3-" = 10),
  defaultWeight = 1
)
p2["F1-F2-F3-"] > 0.5

}
