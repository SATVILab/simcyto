% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{simCytBuildResponseVec}
\alias{simCytBuildResponseVec}
\title{Build a response-shift vector for a binary marker-combination scenario}
\usage{
simCytBuildResponseVec(nMarker, shifts)
}
\arguments{
\item{nMarker}{Integer scalar. Number of binary markers.}

\item{shifts}{Named numeric vector mapping cluster label strings to additive
probability shifts. Cluster labels use the convention \code{"F1-F2+F3-..."}.
Must sum to zero.}
}
\value{
A numeric vector of length \code{2^nMarker} whose named entries give the
additive probability shifts for each cluster.
}
\description{
Construct a response-shift vector of length \code{2^nMarker} for use as an
element of \code{probResponseVecByStimCondition} in \code{\link[=simCytExperiment]{simCytExperiment()}}. This
is a convenience helper for scenarios where only a few populations change
in probability under stimulation.
}
\details{
Each named entry in \code{shifts} specifies the additive change in probability
for the corresponding cluster. Unspecified clusters receive \code{0}. The shifts
must sum to zero so that the resulting stimulated probability vector remains
a valid probability distribution (the caller is responsible for ensuring that
adding the shift vector to \code{probVecUns} yields a non-negative vector summing
to 1).
}
\examples{
# Shift 10 \% probability from the all-negative to the all-positive population
nMarker <- 3
sc <- simCytBuildScenario(nMarker = nMarker)
neg <- sc$clusterLabelVec[1] # "F1-F2-F3-"
pos <- sc$clusterLabelVec[8] # "F1+F2+F3+"
delta <- simCytBuildResponseVec(
  nMarker = nMarker,
  shifts = setNames(c(-0.1, 0.1), c(neg, pos))
)
sum(delta) # 0

}
